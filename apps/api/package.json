{
  "name": "@modelpedia/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "modelpedia": "workspace:*",
    "hono": "catalog:"
  },
  "devDependencies": {
    "wrangler": "catalog:"
  }
}
